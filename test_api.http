### ============================================================
### 法律顾问 Agent — HTTP 测试文件
### 使用 VS Code REST Client 插件（或 IntelliJ HTTP Client）
### ============================================================

### 变量：服务地址
@baseUrl = http://localhost:8000


### ----------------------------------------------------------
### 1. 健康检查
### ----------------------------------------------------------
GET {{baseUrl}}/api/health


### ----------------------------------------------------------
### 2. 单轮问答（不指定 session，使用默认会话）
### ----------------------------------------------------------
POST {{baseUrl}}/api/chat
Content-Type: application/json

{
  "question": "劳动合同的试用期最长是多久？"
}


### ----------------------------------------------------------
### 3. 多轮对话 — 第1轮（指定 session 开启独立会话）
### ----------------------------------------------------------
POST {{baseUrl}}/api/chat
Content-Type: application/json

{
  "question": "什么是正当防卫？有什么条件？",
  "session_id": "user-002"
}


### ----------------------------------------------------------
### 4. 多轮对话 — 第2轮（同一 session，追问）
### ----------------------------------------------------------
POST {{baseUrl}}/api/chat
Content-Type: application/json

{
  "question": "那防卫过当要怎么判？",
  "session_id": "user-002"
}


### ----------------------------------------------------------
### 5. 多轮对话 — 第3轮（继续追问，省略主语）
### ----------------------------------------------------------
POST {{baseUrl}}/api/chat
Content-Type: application/json

{
  "question": "能举个例子吗？",
  "session_id": "user-002"
}


### ----------------------------------------------------------
### 6. 另一个独立会话（session 不同，历史隔离）
### ----------------------------------------------------------
POST {{baseUrl}}/api/chat
Content-Type: application/json

{
  "question": "治安管理处罚有哪些种类？",
  "session_id": "user-003"
}


### ----------------------------------------------------------
### 7. 跨法律问题
### ----------------------------------------------------------
POST {{baseUrl}}/api/chat
Content-Type: application/json

{
  "question": "民事诉讼法中起诉的条件是什么？"
}


### ----------------------------------------------------------
### 8. 超出法律范围的问题
### ----------------------------------------------------------
POST {{baseUrl}}/api/chat
Content-Type: application/json

{
  "question": "今天天气怎么样？"
}
